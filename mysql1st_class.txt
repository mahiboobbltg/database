

CREATE DATABASE  DATABASENAME;

create taBLE <>(
<COLNAME> DT
<COLNAME> DT,
)

CREATE DATABASE  ITDEF;

create TABLE SEP2023(
NAME TEXT,
BRANCH VARCHAR(255),
BATCH VARCHAR(255),
REFERENCE TEXT
);

INSERT INTO <TABLENAME> VALUES()

INSERT INTO SEP2023   VALUES('MAAHI','MECH','FSD','NAYANA');

MULTIPLE RECORDS

INSERT INTO SEP2023   VALUE
						('SOMU','CS','FSD','NAYANA'),
						
						('BHIMU','IS','FSD','BHIMU'),
						
						('SHAAM','CIVIL','FSD','NAYANA'),
						('NAYANA','CS','FSD','MAAHI')
						

;

UPDATE  TABLENAME SET FIELD=NEW_VALUE;
UPDATE  TABLENAME SET FIELD=NEW_VALUE WHERE NAME='MAAHI';

ALTERING

ALTER TABLE <TNAME > ADD  <FIELD>  <DATATYPE>
ALTER TABLE <TNAME > ADD  DROP  
ALTER TABLE <TNAME>  ADD  MODIFY  <DATATYPE>



DELETE 
DELETE TNAME WHERE FIELD=VALUES
DELETE SEP2023 WHERE NAME=MAAHI;




CONSTRAINS


    UNIQUE	  ===>NOT ALLOW DUPLICATE VALUE AND NULL VALUES
	NOT NULL  ==>EMPTY DATA
	CHECK  	  ===>BASED ON CONDITION IT WILL WORK  EX PNO=10
	PRIMARY KEY ===>IT IS COMBINATION OF UNIQUE AND NOT NULL,
						WE CAN HAVE N NO OF PRIMARY KEY BUT RECONDED 1 IN A TABLE 
		
		
	DEFAULT
	INDEX==>





CREATE TABLE FSD(
ID INT,
FNAME TEXT,
AGE INT,
BRANCH VARCHAR(255)   ,
BATCH VARCHAR(255) DEFAULT 'SEP2023' ,
UNIQUE(ID)
);

CREATE TABLE DEVOPS(
ID INT,
FNAME TEXT,
AGE INT,
BRANCH VARCHAR(255)   ,
BATCH VARCHAR(255) DEFAULT 'SEP2023' ,
UNIQUE(ID)
);
 



INSERT INTO FSD   VALUES
						(1,'SOMU',22,'CS','SEP2023'),
						
						(2,'BHIMU',23,'FSD','SEP2023'),
						
						(3,'SHAAM',23,'CIVIL','SEP2023'),
						(4,'NAYANA',23,'CS','SEP2023')
						

;


INSERT INTO FSD VALUES (5,'SOMU','22','IS','SEP2023');



























